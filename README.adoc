= Mail Server Application
:toc:
:toclevels: 3

== Overview

The Mail Server Application is a web-based email client that allows users to send, receive, and manage emails
through a modern web interface.
The application communicates with the THM Mail Server using the SMTP and IMAP protocols.
The frontend is implemented with Angular, while the backend is built with Spring Boot and Kotlin.
Email attachments are stored in SeaweedFS, and application data is persisted in PostgreSQL.
Automated tests use an H2 in-memory database.

== Features

* User authentication
* Sending emails via SMTP
* Receiving and reading emails via IMAP
* Inbox management
* Uploading and downloading email attachments
* Storage of attachments in SeaweedFS
* REST API for frontend-backend communication
* Containerized infrastructure using Docker

== Architecture

The application follows a client-server architecture.
....
+----------------+
| Angular Client |
+----------------+
         |
         | REST API
         v
+----------------------+
| Spring Boot Backend  |
|       (Kotlin)       |
+----------------------+
      |           |
      |           |
      v           v
+-----------+  +-----------+
| PostgreSQL|  | SeaweedFS |
+-----------+  +-----------+
                    ^
                    |
             Email Attachments
      |
      v
+----------------+
| THM Mail Server|
+----------------+
     SMTP / IMAP

Tests:
|
+--> H2 In-Memory Database
....

=== Frontend

The frontend is implemented using Angular and provides the graphical user interface for email management.

=== Backend

* The backend is implemented using Spring Boot and Kotlin.
* It handles business logic, user management, communication with the mail server, and interaction with storage systems.

=== Mail Communication

The application communicates with the THM Mail Server using:

* SMTP (Simple Mail Transfer Protocol) for sending emails
* IMAP (Internet Message Access Protocol) for retrieving and managing emails

=== Data Storage

* PostgreSQL is used as the database for persistent application data.
* SeaweedFS is used for storing email attachments separately from application data.

* H2 is used for testing as an in-memory database.

== Technologies

=== Frontend

* Angular
* TypeScript
* Node.js

=== Backend

* Spring Boot
* Kotlin
* Gradle

=== Infrastructure

* Docker
* PostgreSQL
* SeaweedFS

=== Testing

* H2 Database

== Prerequisites

Before running the application, make sure the following software is installed:

* Java 21
* Gradle
* Node.js
* npm
* Angular CLI
* Docker
* Docker Compose

Verify your installation:

[source,bash]
----
java --version
gradle --version
node --version
npm --version
docker --version
docker compose version
----

== Setup

=== Configure Environment

Create the required configuration files and provide credentials for:

* THM Mail Server
* PostgreSQL
* SeaweedFS

Refer to the project configuration files for the required environment variables.

== Environment variables

=== Backend

These variables are read via `${ENV_VAR:default}` (defaults shown in parentheses):

- `DB_URL` (default: `jdbc:h2:mem:testdb`)
- `DB_DRIVER` (default: `org.h2.Driver`)
- `DB_USER` (default: `dbuser`)
- `DB_PASSWORD` (default: `password`)
- `DB_DDL_AUTO` (default: `create-drop`)

- `APP_SECRET` (default: `default_secret_key`)
- `APP_NAME` (default: `MyApp`)
- `APP_JWT_EXPIRES` (default: `3600`) — seconds
- `APP_JWT_SECRET` (default: `jwt_secret_key`)

- `FILE_UPLOAD_DIR` (default: `uploads`)

===Mail (SMTP / IMAP)

**SMTP (Outbound)** — for sending mails from the support inbox to all registered users:

- `SPRING_MAIL_HOST` (default: `""` — empty, disables SMTP)
- `SPRING_MAIL_PORT` (default: `465`)
- `SPRING_MAIL_USERNAME` — your THM email or mail account
- `SPRING_MAIL_PASSWORD` — corresponding password
- `MAIL_FROM_ADDRESS` (default: uses `SPRING_MAIL_USERNAME`)

**IMAP (Inbound)** — for polling external mail inbox (e.g., THM support mailbox):

- `MAIL_IMAP_HOST` (default: `""` — empty, disables IMAP polling)
- `MAIL_IMAP_PORT` (default: `993`)
- `MAIL_IMAP_USERNAME` — your THM email or mail account
- `MAIL_IMAP_PASSWORD` — corresponding password
- `MAIL_IMAP_FOLDER` (default: `INBOX`)
- `MAIL_IMAP_POLL_INTERVAL_MS` (default: `300000` — 5 minutes)

**Docker Compose**

Used to configure the Postgres container:

- `DB_USER`
- `DB_PASSWORD`
- `DB_NAME`

===Example `.env`

```env
# -------------------------
# Database (Docker Compose)
# -------------------------
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mail_project

# -------------------------
# Backend datasource config
# -------------------------
# For local Postgres (matches docker-compose port mapping 5432:5432)
DB_URL=jdbc:postgresql://localhost:5432/mail_project
DB_DRIVER=org.postgresql.Driver

# JPA schema strategy (dev vs prod)
DB_DDL_AUTO=update

# -------------------------
# App / JWT
# -------------------------
APP_NAME=mail-project
APP_SECRET=change_me_super_secret
APP_JWT_EXPIRES=3600
APP_JWT_SECRET=change_me_jwt_secret

# -------------------------
# File uploads
# -------------------------
FILE_UPLOAD_DIR=uploads

# -------------------------
# SMTP Outbound Mail (Support Notifications)
# -------------------------
SPRING_MAIL_HOST=mailgate.thm.de
SPRING_MAIL_PORT=465
SPRING_MAIL_USERNAME=your-thm-email@student.thm.de
SPRING_MAIL_PASSWORD=your-thm-password
MAIL_FROM_ADDRESS=your-thm-email@student.thm.de

# -------------------------
# IMAP Inbound Mail (External Mailbox Polling)
# -------------------------
MAIL_IMAP_HOST=mailgate.thm.de
MAIL_IMAP_PORT=993
MAIL_IMAP_USERNAME=your-thm-email@student.thm.de
MAIL_IMAP_PASSWORD=your-thm-password
MAIL_IMAP_FOLDER=INBOX
MAIL_IMAP_POLL_INTERVAL_MS=300000
```

== Running the Infrastructure

Start PostgreSQL and SeaweedFS using Docker Compose:

[source,bash]
----
docker compose up -d
----
Verify that all containers are running:

[source,bash]
----
docker ps
----
== Running the Backend

Navigate to the backend directory:

[source,bash]
----
cd backend
----
Start the Spring Boot application:

[source,bash]
----
./gradlew bootRun
----
The backend will be available at:
http://localhost:8080

== Running the Frontend

Navigate to the frontend directory:

[source,bash]
----
cd frontend
----

Install dependencies:

[source,bash]
npm install

Start the Angular development server:

[source,bash]
ng serve

The frontend will be available at:
http://localhost:4200

== Development

=== Backend Development

Build the backend:
[source,bash]
 ./gradlew build

Run all tests:
[source,bash]
 ./gradlew test

=== Frontend Development

run Frontend:
[source,bash]
cd frontend
npm install
npm run build

== Project Structure

....
project-root/
├── backend/
│   ├── src/
│   ├── build.gradle.kts
│   └── ...
│
├── frontend/
│   ├── src/
│   ├── package.json
│   └── ...
│
├── gradle/
├── docker-compose.yml
└── README.adoc
....

== Dependencies

=== Runtime Dependencies

* Spring Boot
* Kotlin
* Angular
* PostgreSQL
* SeaweedFS
* Docker

=== Development Dependencies

* Java 21
* Gradle
* Node.js
* npm
* Angular CLI

=== Test Dependencies

* H2 Database

== License

This project is part of a university project.
